use strict;
use warnings;

# Read the next line
#my $line = <STDIN>;

# and Chomp it
#chomp($line);

# Read line from stdin and chomp it
#chomp($line = <STDIN>);

# Read a line of input and see if its true
while(<STDIN>) {
	
	print "I saw $_";	
}